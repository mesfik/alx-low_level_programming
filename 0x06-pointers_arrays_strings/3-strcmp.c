#include "main.h"
/**
 * _strcmp - function that compares two strings
 *
 * @s1: entry string name
 * @s2: entry string name
 *
 * Return: 0 or s1 greater than s2 or s2 greater than s1
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
